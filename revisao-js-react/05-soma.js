import { areaQuadrado, areaTriangulo } from "./04-areaQuadrado.js" 

export const soma = (a, b) => {
    return a + b
}

export const subtracao = (a,b) => {
    return a - b
}

// console.log(areaQuadrado(10))
// console.log(areaTriangulo(12,55))

export default soma;
// default - apenas o arquivor que tiver ele será exportado